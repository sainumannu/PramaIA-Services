# Requirements per PramaIA-LogService

# Framework web
fastapi>=0.68.0
uvicorn>=0.15.0

# Per gestire richieste HTTP
httpx>=0.19.0
requests>=2.26.0

# Supporto per form e autenticazione
python-multipart>=0.0.5
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Gestione variabili d'ambiente
python-dotenv>=0.19.0
pydantic>=1.8.2
# IMPORTANTE: Necessario per il supporto di BaseSettings in Pydantic v2
# Se si usa Pydantic v2 senza questo pacchetto, il servizio non si avvierà
pydantic-settings>=2.0.0

# Supporto per Jinja2 templates (dashboard web)
jinja2>=3.0.1

# Utilità
pytz>=2021.1
uuid>=1.30

# Librerie di sviluppo (opzionali)
pytest>=6.2.5
black>=21.8b0
flake8>=3.9.2
mypy>=0.910
